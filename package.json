{
  "name": "iles-app",
  "version": "0.0.0",
  "scripts": {
    "dev": "iles dev --open",
    "build": "iles build",
    "preview": "iles preview --open --port 5050",
    "typecheck": "vue-tsc --noEmit",
    "iles:info": "iles info"
  },
  "engines": {
    "node": "^14.18 || >= 16.0.0"
  },
  "devDependencies": {
    "iles": "^0.8.7",
    "typescript": "^4.9.5",
    "vue-tsc": "^0.38.9"
  }
}
